<UserControl x:Class="Library.Views.LibrariesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Library.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="400"/>
        </Grid.ColumnDefinitions>

        <ListBox Grid.Column="0" Grid.Row="0" Height="400" Name="ListBoxLibraries" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#454545" BorderThickness="0 0 1 0" SelectionChanged="MyListViewLibrariesChanged" Margin="10">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                        <TextBlock Text="{Binding Name}" Foreground="#EDF4F2" Margin="10 10 15 15"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Column="0" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center"  Margin="10">
            <Button x:Name="EditButton" Content="Upraviť" FontSize="18" Click="Edit_Library" IsEnabled="False" Margin="5" Padding="5"/>
            <Button x:Name="DeleteButton" Content="Vymazať" FontSize="18" Click="Delete_Library" IsEnabled="False" Margin="5" Padding="5"/>
            <Button x:Name="AddButton" Content="Pridať knižnicu" FontSize="18" Click="Add_Library" Margin="5" Padding="5"/>
        </StackPanel>

        <ListBox Grid.Row="0" Grid.Column="1" Height="400" Name="ListBoxBooks" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#454545" SelectionChanged="MyListViewBooksChanged" BorderThickness="0 0 0 0" Margin="10">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                        <Image Width="50" Height="80" Source="{Binding Picture}" Margin="10 10 15 15"/>
                        <TextBlock Text="{Binding Title}" Foreground="#EDF4F2" Margin="10 10 15 15" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Sector}" Foreground="#EDF4F2" Margin="10 10 15 15" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Author}" Foreground="#EDF4F2" Margin="10 10 15 15" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Year}" Foreground="#EDF4F2" Margin="10 10 15 15" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding ISBN}" Foreground="#EDF4F2" Margin="10 10 15 15" VerticalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button x:Name="EditBook" Content="Upraviť" FontSize="18" Click="Edit_Book" IsEnabled="False" Margin="5" Padding="5"/>
            <Button x:Name="DeleteBook" Content="Vymazať" FontSize="18" Click="Delete_Book" IsEnabled="False" Margin="5" Padding="5"/>
            <Button x:Name="AddBook" Content="Pridať knihu" FontSize="18" Click="Add_Book" IsEnabled="False" Margin="5" Padding="5"/>
        </StackPanel>
    </Grid>
</UserControl>
